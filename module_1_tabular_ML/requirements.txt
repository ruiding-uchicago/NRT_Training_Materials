# Module 1 dependencies. Loose lower-bounds so it also works on Google Colab.
# Exact metrics (XGBoost test R2 ~= 0.84) may drift by ~0.01 across library versions.
numpy>=1.23
pandas>=1.4
scikit-learn>=1.1
xgboost>=1.6
shap>=0.41
matplotlib>=3.4
seaborn>=0.11
